# 项目计划与当前进度

## 2026-04-28 稳定性收尾进度更新

本项目已经完成 WeChatAuto 稳定性与一致性增强主线的 Round 3-20。当前状态不再只是“桌面客户端产品化进行中”，而是已经进入“核心闭环基本完成，准备最终验收和交付整理”的阶段。

后续开发、收尾优先级和验收标准已经统一收敛到：[docs/后续开发计划与收尾路线图.md](./docs/后续开发计划与收尾路线图.md)。本文保留项目总体进度记录，具体下一步执行以路线图为准。

当前更准确的进度判断：

- 后端运行时主链路：`88% ~ 92%`
- 真实发送一致性与恢复能力：`85% ~ 90%`
- 桌面客户端服务层：`85% ~ 90%`
- 前后端产品化闭环：`75% ~ 82%`
- 安全策略与人工审核闭环：`80% ~ 88%`
- RAG 可信化链路：`78% ~ 85%`
- 文档与最终交付体系：`65% ~ 72%`

已经新增并稳定下来的能力：

- SQLite runtime state 已承接 `MessageEvent / ReplyJob / SendJob / SendAttempt` 等一致性状态。
- `SendCoordinator` 已接管真实发送保护，发送前检查、发送后确认、失败状态记录已经形成闭环。
- `SEND_UNCERTAIN` 明确为不可自动重发状态，并进入人工处理台。
- 安全策略已规则化和配置化，高风险消息默认进入人工审核。
- Pending 审核页已经能展示风险等级、原因码，并支持批准后进入发送协调器。
- RAG evidence、acceptance history、trust diagnostics、trusted rebuild 已经形成知识库可信化闭环。
- 统一桌面验收脚本 `scripts/run_desktop_acceptance_flow.py` 已可用于收尾前的安全验收。

仍需收尾的内容：

- 更新所有用户文档和接口说明，清理历史乱码。
- 将 RAG evidence 进一步接入 Pending 审核页。
- 完善 `SEND_UNCERTAIN` 处理台的筛选、审计和趋势展示。
- 做最终 release checklist 和真实微信长跑验收。

## 项目当前所处阶段

这个项目已经完成了“从单点自动回复脚本到可扩展运行时”的核心过渡。

现在的状态更适合定义为：

`后端核心基本可用，桌面客户端产品化进行中。`

可以把整体进度粗略理解为：

- 后端运行时主链路：`80% ~ 85%`
- 真实微信守护与联调能力：`70% ~ 80%`
- 桌面客户端服务层：`65% ~ 75%`
- 前后端完整产品化闭环：`50% ~ 60%`
- 文档与运维体系：`65% ~ 75%`

## 已完成

### 1. 微信自动回复主链路

- 单聊自动回复
- 群聊 `@` 自动回复
- 全局轮询未读会话
- 多条未读消息聚合回复
- 未读顺序重排与回归测试

### 2. 编排与提示词链路

- `Message` 标准化
- 上下文裁剪
- Prompt 构建
- Reply pipeline
- provider 调用边界整理

### 3. 身份与画像

- 用户身份识别
- identity draft / candidate 管理
- 用户画像读写
- 助手画像读写
- 分层自我身份
  - 全局
  - 关系
  - 用户覆盖

### 4. 知识库与检索

- 本地知识库导入
- 索引重建
- 文档切块
- 本地检索
- 联网扩库基础能力
- 支持 `txt/md/json/pdf/docx/图片 OCR`

### 5. 记忆与可观测性

- 结构化 JSONL 日志
- memory store
- 会话快照写回
- 日志查看脚本
- memory 查看脚本
- 守护模式心跳与 backoff

### 6. 桌面客户端后端壳层

- `DesktopAppService`
- 设置读写
- daemon 状态管理
- 客户列表与身份草稿聚合
- 自我身份管理接口
- 知识库导入接口

## 已完成但仍需真实联调

### 1. 真实微信启动链路

已经有：

- 首次启动引导脚本
- 讲述人接入
- 正式监听脚本切换

但还需要继续验证：

- 不同窗口状态下的一致性
- 首启和日常启动切换体验
- 更稳定的停止与恢复行为

### 2. 真实微信运行稳定性

已经解决过的问题包括：

- WMI 检测兜底
- UTF-8 控制台输出
- 未读消息顺序
- 部分群 sender 透传

仍需继续观察：

- 长时间运行下的 UI 漂移
- 特殊消息形态
- 微信窗口切换与焦点异常

## 尚未完成

### 1. 桌面客户端完整闭环

还缺：

- 前端页面与后端服务层完整接线
- 桌面壳层集成
- 托盘、静默后台、定时启停的完整产品化体验

### 2. 会话页真实能力闭环

`DesktopAppService` 中仍有部分接口没有真正接到微信运行时，例如：

- `suggest_reply(...)`
- `send_reply(...)`

它们现在仍偏向桌面页预留接口，而不是完整业务闭环。

### 3. 运维与长期运行能力

还需要继续补：

- 日志轮转与保留策略
- memory 裁剪与保留策略
- 失败恢复策略说明
- 备份与迁移说明
- 更完整的运行手册

### 4. 安全边界

还需要继续补：

- 更明确的敏感信息处理规则
- 更稳的人工接管策略
- 高风险消息的保护边界

## 下一阶段重点

下一阶段不再在本文维护重复清单，统一以 [docs/后续开发计划与收尾路线图.md](./docs/后续开发计划与收尾路线图.md) 为准。

当前推荐顺序：

1. 真实微信长时间运行验收和自动回复误拦截回归。
2. 安全词配置页面优化、审核原因中文化、审核发送结果增强。
3. 知识库证据接入审核页、可信重建体验完善。
4. 运行时模块拆分。
5. 用户操作手册、备份迁移说明和发布清单统一。

## 文档导航

- [README.md](./README.md)
  项目入口与当前能力总览
- [docs/后续开发计划与收尾路线图.md](./docs/后续开发计划与收尾路线图.md)
  后续开发优先级、剩余任务和验收标准
- [docs/architecture-overview.md](./docs/architecture-overview.md)
  当前代码结构、模块边界、数据流与存储布局
- [docs/desktop-app-backend.md](./docs/desktop-app-backend.md)
  客户端后端接口说明
- [docs/superpowers/README.md](./docs/superpowers/README.md)
  历史设计与阶段实现归档
